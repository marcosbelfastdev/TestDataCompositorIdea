package testData.flows.greenFlow;

import testData.TestDataCompositor;

public class GreenFlow extends TestDataCompositor {

    /*
    All green flow test cases have in common:
    - an account with branch number, account number, a bank and a tax id either
    for a physical or legal person.
    - a search field appears in the flow, where we want to search for the bank name
    - a validation occurs that we need both bank code and name
     */

}
